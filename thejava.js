var data = [];

function setup() {
  createCanvas(outerWidth, outerHeight);
  background(255, 0, 200);
}

//function mousePressed() {
    //var x = Map(mouseX, 0, with, 0, 1);
    //var y = Map(mouseY, 0, ,)
//}